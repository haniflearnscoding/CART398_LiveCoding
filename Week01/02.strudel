setcps(70/60/4)

stack(
  // deeper 808 - pitched down, filtered, saturated
  s("bd:6 ~ ~ bd:20 ~ bd:20 ~ ~")
    .speed(0.7)
    .lpf(60)
    .shape(0.4)
    .gain(1.5),

  // sparse triplet hats with rolls
  s("~ hh ~ hh ~ hh [hh hh hh] ~")
    .gain(.6).pan(sine.slow(4)),

  // perc for texture
  s("perc:3(3,8)").gain(0.4).delay(0.5).delayfeedback(0.6)

).bank("RolandTR808")
  .room(0.4)
  .orbit(1)
